# Telegram bot
python-telegram-bot==20.6

# Scheduler for auto-spawn, daily tasks
apscheduler==3.10.6

# Image processing (inventory, battle visuals)
Pillow==10.0.1

# Database (future / optional persistence)
psycopg2-binary==2.9.7

# Web requests / image download
requests==2.31.0

# Environment variables loader
python-dotenv==1.1.1

# Terminal / message coloring
colorama==0.4.6

# Build helpers for Render
setuptools>=65.0.0
wheel
